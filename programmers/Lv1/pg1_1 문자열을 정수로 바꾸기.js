// 1. 문자열을 정수로 바꾸기 
// 문자열 s를 숫자로 변환한 결과를 반환하는 함수, solution을 완성하세요.

function solution(s) {
    var answer = 0;
    
    // 1)
    answer = Number(s)

    // 2)
    answer = s/1

    // 3)
    answer = +s
    
    return answer;
}
